<Window x:Class="CubeHack.Editor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:CubeHack.Editor"
        Title="CubeHack Editor"
        Height="600"
        Width="800">
    <Window.InputBindings>
        <KeyBinding Modifiers="Ctrl"
                    Key="S"
                    Command="{Binding SaveCommand}" />
        <KeyBinding Key="F5"
                    Command="{Binding RunCommand}" />
    </Window.InputBindings>
    <DockPanel>
        <ToolBar DockPanel.Dock="Top"
                 ToolBarTray.IsLocked="True">
            <Button Margin="2"
                    Command="{Binding SaveCommand}"
                    ToolTip="Save the mod file (Ctrl+S)">Save</Button>
            <Button Margin="2"
                    Command="{Binding RunCommand}"
                    ToolTip="Run the game (F5)">Run</Button>
        </ToolBar>
        <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Visible">
            <local:ItemEditor Item="{Binding ModItem}" />
        </ScrollViewer>
    </DockPanel>
</Window>
